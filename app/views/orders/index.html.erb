<!DOCTYPE html>
<html>
<head>
  <title>Order Index</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <p style="color: green"><%= notice %></p>
        <h1>Orders</h1>
      </div>

      <% @orders.each do |order| %>
        <div class="col-sm-12 col-md-6 col-lg-4 mb-3">
          <div class="card border border-2 border-success p-1" style="width: 18rem;">
            <div class="card-header">
              <h5 class="card-title"><%= order.product_name %></h5>
            </div>
            <div class="card-body">
              <p class="card-text">Product Count: <%= order.product_count %></p>
              <p class="card-text">Customer: <%= order.customer.full_name %></p>
            </div>
            <div class="card-footer d-flex justify-content-center">
              <%= link_to 'Show', order, class: "btn btn-light btn-sm rounded-1 me-2" %>
              <%= link_to 'Edit', edit_order_path(order), class: "btn btn-light btn-sm rounded-1 me-2" %>
              <%= link_to 'Delete', order, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-sm rounded-1" %>
            </div>
          </div>
        </div>
      <% end %>

      <div class="col-12 mt-3">
        <%= link_to 'New Order', new_order_path, class: "btn btn-success btn-sm rounded-1" %>
      </div>
    </div>
  </div>

  <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
